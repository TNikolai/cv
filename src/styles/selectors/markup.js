import { path } from 'ramda';

const startDirection = path(['theme', 'markup', 'startDirection']);
const endDirection = path(['theme', 'markup', 'endDirection']);
const dir = path(['theme', 'markup', 'dir']);

export { startDirection, endDirection, dir };
