import styled from "@emotion/styled";

const Styled = {};

Styled.Socials = styled.div`
  display: flex;
  justify-content: space-evenly;
  gap: 0.5rem 0.5rem;
  flex-wrap: wrap;
  margin: 1rem;
`;

Styled.Img = styled.img`
  width: 2rem;
`;

export default Styled;
